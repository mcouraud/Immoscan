<div class="container">
  <h1>Résultats</h1>
  <p class="text-center"> Les logements à <%= params[:city_query] %></p>

  <% @flats.each do |flat| %>
  <p> <%= flat.address %> | <%= flat.price %>€/mois | T<%= flat.nb_rooms %></p>
  <% end %>

</div>
